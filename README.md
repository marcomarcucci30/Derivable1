# Derivable1
Per eseguire l'applicazione, lanciare il main della classe `RetrieveTicketsID.java`. Nel file di configurazione `config.properties`, è possibile selezionare il progetto a cui far riferimento, modificando il valore della proprietà `PROJECT`.
